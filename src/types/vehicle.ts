export enum VehicleType {
    TRUCK = 'truck',
    VAN = 'van',
    BIKE = 'bike',
}

export enum VehicleStatus {
    AVAILABLE = 'available',
    ON_TRIP = 'on_trip',
    IN_SHOP = 'in_shop',
    RETIRED = 'retired',
}
